# oferta — Honest Run report — 2026-06-14

## Run summary
First end-to-end, gated, logged run of the engine: the `oferta` recipe (the Chapter-11 Bayesian role scorer) on the verified sample fixture, in **sample mode** (no live network, no external writes, zero model calls). Run id `oferta-2026-06-14-001`.

## Purpose
Close gap #4 — prove a recipe can complete a run that passes the phase gates and emits a sourced, inspectable decision rather than model judgment.

## Source inventory
- `data/examples/ch11-roles.json` — verified worked-example roles (Ch.7/11).
- `data/examples/run-envelope.json` — run declaration, `mode: sample`.
- `scripts/score/role-scorer.mjs` — the stored combiner (no ad-hoc code).

## Inputs used
5 roles from the fixture. No external fetch; pure local computation.

## Phase-gate results
| Gate | Result |
|---|---|
| 1 Source | ✓ inputs + script present |
| 2 Scope | ✓ `mode: sample` declared |
| 3 Data-shape | ✓ input + outputs parse |
| 4 Script-readiness | ✓ `scripts/score/role-scorer.mjs` exists |
| 5 Approval | n/a — no live network / external write / sensitive model call in sample mode |
| 6 Report | ✓ this report + `logs/oferta-2026-06-14.json` |
| Human adequacy | **PENDING — your attestation** |

## Steps completed
verify-provenance → ingest-inputs → validate-data-shape → score-composite → produce-report.

## Records seen / rejects / duplicates
5 seen · 0 rejects · 0 duplicates.

## Verified findings (sourced — every term traces to record / model-judgment / your-input)
| Role | Composite | Rec |
|---|---|---|
| Cambridge biotech (Proven) | 0.446 | **Apply** |
| Likely-tier sponsor | 0.418 | **Consider** (soft spot: tier "Likely") |
| Non-sponsor, HM is a contact | 0.193 | **Apply ⟵ documented override** |
| Household non-sponsor | 0.178 | **Skip** |
| Proven sponsor, ghost posting | 0.000 | **Skip** (liveness gate closed) |

Full term-by-term audit: `data/examples/role-scores.md`. Cambridge 0.446 / non-sponsor 0.178 reproduce Ch.11's worked example exactly.

## Flags
- **Skip rate 40%** — below the ~50% a healthy run skips. Expected here: a 5-role curated fixture, not a live search.
- **`role_quality` weight = 0 [VERIFY]** — the Chapter-9 role-quality signal contributes nothing to any composite (gap #3 finding). Pin this before real decisions.
- **1 documented override** — the "HM is a contact" role flips Skip(0.193)→Apply with a recorded reason (override discipline, Ch.11).

## Typed TODOs
None raised by this run.

## Human approvals
None required (sample mode). Promotion of `oferta` past `DRAFT` requires your adequacy attestation below.

## Inferred findings
None — every output is a record or a labeled judgment; no unsourced inference was emitted.

## Decision recommendation
The run is **machine-clean** (all automatable gates pass; output is fully sourced and reproduces the book). It is **not yet adequacy-attested**. To promote `oferta` past DRAFT: review this report + `data/examples/role-scores.md`, confirm the decisions are adequate, and record your attestation (name + date) in `recipes/oferta.md` frontmatter and `logs/RUN_LOG.md`.
